# Curio — Codex Entry Point

**Read `HARNESS.md` first** — it is the canonical, provider-neutral operating contract. Everything below is Codex-specific.

After `HARNESS.md`, read `providers/codex/overrides.md` for Codex-specific layout (compatibility skill mirror, plugin manifest) and notes.

## Quick Start (Codex only)

- `.agents/skills/` mirrors `skills/` for Codex's loader.
- Use `curio agent print-env codex` for the full environment contract.
