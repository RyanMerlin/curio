# Curio — Gemini Entry Point

**Read `HARNESS.md` first** — it is the canonical, provider-neutral operating contract. Everything below is Gemini-specific.

After `HARNESS.md`, read `providers/gemini/overrides.md` for Gemini-specific runtime notes (ADK-Go launcher).

## Quick Start (Gemini only)

- Initial integration target is the Go ADK launcher (`CURIO_GEMINI_RUNTIME=adk-go`).
- Use `curio agent print-env gemini` for the full environment contract.
